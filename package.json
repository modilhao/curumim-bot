{
  "name": "agencia-oca-assistente-pro",
  "version": "1.0.0",
  "description": "An AI-powered chat assistant for the 'agencia OCA' digital marketing agency.",
  "private": true,
  "scripts": {
    "dev": "echo 'Run index.html in a live server'",
    "build": "mkdir public && cp index.html index.tsx App.tsx metadata.json types.ts constants.ts public/ && cp -r components services public/"
  },
  "dependencies": {
    "@google/genai": "^1.20.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5"
  }
}